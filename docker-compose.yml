services:
  server:
    build: ./AREA_ReST_API/AREA_ReST_API
    ports:
      - "8080"
    restart: on-failure

  client_web:
    build: ./client_web
    ports:
      - "8081"
    volumes:
      - clients
    restart: always

  client_mobile:
    build: ./mobile
    volumes:
      - clients
    restart: always
    image: 'flutter'


volumes:
  clients:
